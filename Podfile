# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Freeze AI POS' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Freeze AI POS

	pod 'SwiftyJSON', '~> 4.0'

  target 'Freeze AI POSTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'Freeze AI POSUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
